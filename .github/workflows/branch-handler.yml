name: Minimal Test

on:
  workflow_dispatch:
    inputs:
      test_input:
        description: Test string
        required: true

jobs:
  test-run:
    runs-on: ubuntu-latest
    steps:
      - name: Echo Input
        run: echo "Input was: ${{ github.event.inputs.test_input }}"
